Some naive attempts to encode PTS, so as to have a general CR(beta) result.

Pseudoterms are easy (and that's what matters), but typing isn't, at all,
see the failed attempt for \lambda\underline{\omega}

So, does this make any sense?

Also, a naive approach to counterexample search